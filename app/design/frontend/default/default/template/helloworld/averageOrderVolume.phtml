<style>
  #format { margin-top: 2em; }
</style>

<link rel="stylesheet" href="//code.jquery.com/ui/1.11.2/themes/smoothness/jquery-ui.css">

<script type="text/javascript" src="https://www.google.com/jsapi"></script>
<script type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.js"></script>
<script type="text/javascript" src="https://code.jquery.com/ui/1.11.1/jquery-ui.js"></script>

<script type="text/javascript" src="//cdn.datatables.net/1.10.4/js/jquery.dataTables.min.js"></script>


    <script type="text/javascript">

        function getData() {
            
            $.ajax({
                type: "POST",
                url: "<?php echo Mage::getUrl('reports/data/getAverageOrderSize'); ?>",
                dataType: 'json',
                success: function(data) {

                    var newData = new Array();

                    var header = new Array(2);
                    header[0] = 'Periodo';
                    header[1] = 'Monto [ARS]';
                    
                    newData.push(header);

                    for (var i = 0; i < data.length; i++) {

                        var newRow = new Array();
                    
                        newRow[0] = data[i].period;
                        newRow[1] = parseFloat(data[i].qtyPerOrder);
                        
                        newData.push(newRow);
                        
                        
                    }

                    drawChart(newData);
                    
                    createTable(data);

                }
            });
            
        }

        google.load("visualization", "1", {
            packages: ["corechart"]
        });
        
        google.setOnLoadCallback(getData);

        function drawChart(rawData) {
            var data = google.visualization.arrayToDataTable(rawData);

            var options = {
                title: 'Promedio cantidad de items por orden'
            };

            var chart = new google.visualization.LineChart(document.getElementById('chart_div'));

            chart.draw(data, options);
        }
        
        function createTable(jsonData) {
            $('#avg_order_size_table').dataTable({
                "aaData": jsonData,
                "aoColumns": [
                                { "mDataProp": "period" },
                                { "mDataProp": "qtyPerOrder" },
                                { "mDataProp": "qty" },
                                { "mDataProp": "amount" }
                            ]
            });
        }

        $(document).ready(function() {

            var menuLink = document.getElementById("menu_average_order_volume");
            menuLink.className = "active";

        });
    
      
    </script>
    
    <div id="chart_div" style="width: 900px; height: 500px;"></div>
    
    <div class="bs-example">
        <table id="avg_order_size_table" class="table table-striped table-bordered" cellspacing="0" width="100%">
            <thead>
                <tr>
                    <th>Periodo</th>
                    <th>Items por Orden</th>
                    <th># Ordenes</th>
                    <th>Monto [ARS]</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
    
    <div id="filters">
        <?php echo $this->getChildHtml('filters'); ?>
    </div>